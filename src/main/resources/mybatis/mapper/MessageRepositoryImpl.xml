<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="team.cms.repository.MessageRepository" >
    <select id="getSentMessageBySenderId" resultType="team.cms.entity.Message">
        select * from message where sender_id=#{senderId}
    </select>
    <select id="getReadMessageByRecipientId" resultType="team.cms.entity.Message">
        select * from message where recipient_id=#{recipientId} and 'read'=true
    </select>
    <select id="getUnreadMessageByRecipientId" resultType="team.cms.entity.Message">
        select * from message where recipient_id=#{recipientId} and 'read'=false
    </select>
    <insert id="insertMessageByRecipientId" useGeneratedKeys="true" keyProperty="id">
        insert into message(sender_id, recipient_id, content, send_time, 'read') values (#{sendId},#{recipientId},
        #{content},#{sendTime},false)
    </insert>
</mapper>